# Two live git worktrees for other branches -- full repo checkouts hidden only by
# .git/info/exclude and the global ~/.gitignore, neither of which Prettier reads.
.claude/

# Generated: an esbuild bundle whose own header says do not edit, and a design-tool export.
design/mission-control/support.js
design/mission-control/MissionControl.dc.html

# Regenerated by scripts/sanitize-fixture.mjs; formatting it churns on every capture run.
e2e/fixtures/live-snapshot.ts

# One 1,466-byte line of captured sensor arrays.
e2e/fixtures/grow-light-data.json

# pnpm owns the lockfile's formatting.
pnpm-lock.yaml

# Belt and braces: build and cache output, in case anyone passes --ignore-path, which replaces
# rather than supplements the .gitignore default.
.svelte-kit/
build/
build-recorder/
coverage/
playwright-report/
test-results/
.playwright/
.playwright-mcp/
.pnpm-cache/
.pnpm-data/
.pnpm-state/
data/
